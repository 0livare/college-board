info:
  name: Exam item audit trail
  type: http
  seq: 5

http:
  method: GET
  url: "{{appUrl}}/api/items/{{examItemId}}/audit"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
