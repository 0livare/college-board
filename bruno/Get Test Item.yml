info:
  name: Get Test Item
  type: http
  seq: 2

http:
  method: GET
  url: "{{appUrl}}/api/items/{{testItemId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
