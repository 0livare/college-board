info:
  name: List exam items
  type: http
  seq: 3

http:
  method: GET
  url: "{{appUrl}}/api/items"
  body:
    type: json
    data: |-
      {
        "limit": 1,
        "offset": 0
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
